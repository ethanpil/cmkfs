version: "2"

linters:
  default: standard
  settings:
    errcheck:
      exclude-functions:
        - syscall.Close
        - (*os.File).Close
        - (io.Closer).Close
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
